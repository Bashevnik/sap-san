/* ============================================================
   SAP SAN — КОНФІГ ІНТЕГРАЦІЇ
   ------------------------------------------------------------
   Єдиний файл, який редагує розробник бота/бекенду.
   Більше нічого міняти на фронтенді не треба.

   apiBase — базова адреса API адмінки (Telegram-бот).
     ''  → сайт працює на вбудованому контенті з js/data.js
     'https://…/api' → сайт бере контент і приймає заявки звідти

   Приклад для Vercel:
     apiBase: 'https://sapsan-bot.vercel.app/api'
   ============================================================ */
window.SAPSAN_CONFIG = {
  apiBase: '',

  /* Скільки секунд тримати відповідь /content у пам'яті вкладки */
  cacheTtl: 60,

  /* Скільки чекати на API, перш ніж показати вбудований контент.
     Сайт ніколи не має «висіти» через недоступний бекенд. */
  timeoutMs: 4000
};
